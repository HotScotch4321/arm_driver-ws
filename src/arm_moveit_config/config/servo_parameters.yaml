servo_node:
  ros__parameters:
    # Robot configuration
    move_group_name: "arm"
    planning_frame: "base"
    ee_frame_name: "compound_3"  
    
    # Input/Output topics
    cartesian_command_in_topic: "/servo_node/delta_twist_cmds"
    joint_command_in_topic: "/servo_node/delta_joint_cmds"
    command_out_topic: "/arm_controller/joint_trajectory"
    command_out_type: "trajectory_msgs/JointTrajectory"
    
    # Publishing options
    publish_joint_positions: true
    publish_joint_velocities: true
    publish_joint_accelerations: false
    
    # Control parameters
    publish_period: 0.01  # 100Hz control loop
    incoming_command_timeout: 1.0
    num_outgoing_halt_msgs_to_publish: 4
    
    # Velocity scaling for MG996R servos
    linear_velocity_scaling: 0.5     
    rotational_velocity_scaling: 0.5 
    joint_velocity_scaling: 0.5       

    # Safety limits 
    lower_singularity_threshold: 0.05
    hard_stop_singularity_threshold: 0.02
    leaving_singularity_threshold_multiplier: 2.0
    
    # Collision checking 
    check_collisions: true
    collision_check_rate: 10.0
    self_collision_proximity_threshold: 0.01
    scene_collision_proximity_threshold: 0.02
    
    # Smoothing 
    use_smoothing: true
    smoothing_filter_plugin_name: "online_signal_smoothing::ButterworthFilterPlugin"
    
    # Joint limits enforcement
    enforce_joint_limits: true
    
    # Monitor joint states
    joint_topic: "/joint_states"
    
    # Planning scene
    monitored_planning_scene_topic: "/planning_scene"
    is_primary_planning_scene_monitor: true